- if current_user.admin?
  %p{:style => 'float: left; margin-right: 32px'}
    %label{:for=>"snippet_site_id", :Class => "admin_only"}
      Site
    = select :snippet, :site_id, Site.find(:all).map { |s| [s.name, s.id] }
